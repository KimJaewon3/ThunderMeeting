const { createChats } = require("./createChats")
const { deleteChats } = require("./deleteChats")
const { getChats } = require("./getChats")

module.exports = {
  createChats,
  getChats,
  deleteChats,
}